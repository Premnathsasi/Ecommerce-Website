import { Button } from "react-bootstrap";
const HeaderCartButton = (props) => {
  return (
    <Button variant="dark" className=" border-primary ">Cart</Button>
  );
};

export default HeaderCartButton;
